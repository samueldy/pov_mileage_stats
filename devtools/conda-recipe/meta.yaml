package:
  name: pov_mileage_stats
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - pov_mileage_stats

about:
  home: add_url_here
  license: MIT License
